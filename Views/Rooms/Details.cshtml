@* @model Midterm.Models.Room

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4>Room</h4>
    <hr />
    <dl class="row mt-2">
        <dt class="col-sm-4">
            @Html.DisplayNameFor(model => model.RoomName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.RoomName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.IsHidden)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.IsHidden)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.RoomType)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.RoomType.RoomTypeName)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.RoomId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div> *@






@* @model Midterm.Models.Room

@{
    ViewData["Title"] = "Room Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <!-- Header -->
            <div class="text-center mb-5">
                <h1 class="display-4 fw-bold text-gradient">Room Details</h1>
                <div class="subtitle-line mx-auto"></div>
            </div>

            <!-- Main Card -->
            <div class="room-card position-relative">
                <div class="card-overlay"></div>
                
                <div class="card-content position-relative">
                    <h2 class="room-title">@Model.RoomName</h2>
                    
                    <div class="details-grid mt-5">
                        <div class="detail-item">
                            <span class="detail-label">Name</span>
                            <span class="detail-value">@Html.DisplayFor(model => model.RoomName)</span>
                        </div>
                        
                        <div class="detail-item">
                            <span class="detail-label">Status</span>
                            <span class="detail-value">
                                <span class="status-indicator @(Model.IsHidden ? "hidden" : "visible")">
                                    @Html.DisplayFor(model => model.IsHidden)
                                </span>
                            </span>
                        </div>
                        
                        <div class="detail-item">
                            <span class="detail-label">Type</span>
                            <span class="detail-value highlight">
                                @Html.DisplayFor(model => model.RoomType.RoomTypeName)
                            </span>
                        </div>
                    </div>
                    
                    <!-- Actions -->
                    <div class="action-buttons mt-5">
                        <a asp-action="Edit" 
                           asp-route-id="@Model?.RoomId" 
                           class="btn btn-gradient me-3">
                            <i class="bi bi-pencil"></i> Edit Room
                        </a>
                        <a asp-action="Index" 
                           class="btn btn-outline-elegant">
                            <i class="bi bi-arrow-return-left"></i> Back
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .text-gradient {
        background: linear-gradient(45deg, #2c3e50, #3498db);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
    }

    .subtitle-line {
        width: 100px;
        height: 3px;
        background: linear-gradient(90deg, #3498db, #2c3e50);
        margin-top: 15px;
    }

    .room-card {
        background: #ffffff;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        padding: 2rem;
        overflow: hidden;
        transition: transform 0.3s ease;
    }

    .room-card:hover {
        transform: translateY(-5px);
    }

    .card-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg, rgba(52,152,219,0.1), rgba(44,62,80,0.1));
        opacity: 0.2;
    }

    .room-title {
        font-size: 2.5rem;
        color: #2c3e50;
        font-weight: 700;
        letter-spacing: 1px;
    }

    .details-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 2rem;
    }

    .detail-item {
        background: rgba(255,255,255,0.9);
        padding: 1rem;
        border-radius: 10px;
        backdrop-filter: blur(5px);
    }

    .detail-label {
        display: block;
        font-size: 0.9rem;
        color: #7f8c8d;
        text-transform: uppercase;
        margin-bottom: 0.5rem;
    }

    .detail-value {
        font-size: 1.2rem;
        color: #2c3e50;
        font-weight: 500;
    }

    .status-indicator {
        padding: 0.4rem 1rem;
        border-radius: 20px;
        font-size: 0.9rem;
    }

    .status-indicator.hidden { background: #e74c3c; color: white; }
    .status-indicator.visible { background: #2ecc71; color: white; }

    .highlight {
        color: #3498db;
        font-weight: 600;
    }

    .btn-gradient {
        background: linear-gradient(45deg, #3498db, #2c3e80);
        color: white;
        border: none;
        padding: 0.7rem 1.5rem;
        border-radius: 25px;
        transition: all 0.3s ease;
    }

    .btn-gradient:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(52,152,219,0.4);
        color: white;
    }

    .btn-outline-elegant {
        border: 2px solid #2c3e50;
        color: #2c3e50;
        padding: 0.7rem 1.5rem;
        border-radius: 25px;
        transition: all 0.3s ease;
    }

    .btn-outline-elegant:hover {
        background: #2c3e50;
        color: white;
    }
</style> *@


@model Midterm.Models.Room

@{
    ViewData["Title"] = "Room Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <!-- Header -->
            <div class="text-center mb-5">
                <h1 class="display-4 fw-bold text-gradient">Room Details</h1>
                <div class="subtitle-line mx-auto"></div>
            </div>

            <!-- Main Card -->
            <div class="room-card position-relative">
                <div class="card-overlay"></div>
                
                <div class="card-content position-relative">
                    <h2 class="room-title text-center mb-4">@Model.RoomName</h2>
                    
                    <!-- Table Layout -->
                    <div class="table-responsive">
                        <table class="table table-elegant">
                            <tbody>
                                <tr>
                                    <th scope="row">Room Name</th>
                                    <td>@Html.DisplayFor(model => model.RoomName)</td>
                                </tr>
                                <tr>
                                    <th scope="row">IsHidden</th>
                                    <td>
                                        <span class="status-indicator @(Model.IsHidden ? "hidden" : "visible")">
                                            @Html.DisplayFor(model => model.IsHidden)
                                        </span>
                                    </td>
                                </tr>
                                <tr>
                                    <th scope="row">Room Type</th>
                                    <td class="highlight">
                                        @Html.DisplayFor(model => model.RoomType.RoomTypeName)
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                    
                    <!-- Actions -->
                    <div class="action-buttons mt-5 text-center">
                        <a asp-action="Edit" 
                           asp-route-id="@Model?.RoomId" 
                           class="btn btn-gradient me-3">
                            <i class="bi bi-pencil"></i> Edit Room
                        </a>
                        <a asp-action="Index" 
                           class="btn btn-outline-elegant">
                            <i class="bi bi-arrow-return-left"></i> Back
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .text-gradient {
        background: linear-gradient(45deg, #2c3e50, #3498db);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
    }

    .subtitle-line {
        width: 100px;
        height: 3px;
        background: linear-gradient(90deg, #3498db, #2c3e50);
        margin-top: 15px;
    }

    .room-card {
        background: #ffffff;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        padding: 2rem;
        transition: transform 0.3s ease;
    }

    .room-card:hover {
        transform: translateY(-5px);
    }

    .card-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg, rgba(52,152,219,0.1), rgba(44,62,80,0.1));
        opacity: 0.2;
    }

    .room-title {
        font-size: 2.2rem;
        color: #2c3e50;
        font-weight: 700;
        letter-spacing: 1px;
    }

    .table-elegant {
        border-collapse: separate;
        border-spacing: 0 10px;
    }

    .table-elegant th {
        background: #f8f9fa;
        color: #2c3e50;
        font-weight: 600;
        padding: 1rem 1.5rem;
        border: none;
        width: 30%;
        border-top-left-radius: 10px;
        border-bottom-left-radius: 10px;
    }

    .table-elegant td {
        background: rgba(255,255,255,0.9);
        padding: 1rem 1.5rem;
        border: none;
        border-top-right-radius: 10px;
        border-bottom-right-radius: 10px;
        vertical-align: middle;
    }

    .status-indicator {
        padding: 0.4rem 1rem;
        border-radius: 20px;
        font-size: 0.9rem;
        display: inline-block;
    }

    .status-indicator.hidden { background: #e74c3c; color: white; }
    .status-indicator.visible { background: #2ecc71; color: white; }

    .highlight {
        color: #3498db;
        font-weight: 600;
    }

    .btn-gradient {
        background: linear-gradient(45deg, #3498db, #2c3e80);
        color: white;
        border: none;
        padding: 0.7rem 1.5rem;
        border-radius: 25px;
        transition: all 0.3s ease;
    }

    .btn-gradient:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(52,152,219,0.4);
        color: white;
    }

    .btn-outline-elegant {
        border: 2px solid #2c3e50;
        color: #2c3e50;
        padding: 0.7rem 1.5rem;
        border-radius: 25px;
        transition: all 0.3s ease;
    }

    .btn-outline-elegant:hover {
        background: #2c3e50;
        color: white;
    }
</style>